import { UnderDevelopmentPage } from "../components/UnderDevelopmentPage";

const SensorTypes = () => {
    return (
        <UnderDevelopmentPage 
            featureName="Sensor Types"
            estimatedCompletion="Right after we sensor what you did there"
            funFact="Sensors are like teenagers - they're always sensing something, but half the time what they're telling you doesn't make sense."
        />
    );
};

export default SensorTypes;